"""Fixture corpus for the AI layer's mock adapter.

Per `ai-tool-smith`'s working method: a case belongs here, with the exact
fields it must produce written down alongside it, before any test asserts
against it - the assertion is the point, not the fixture.
"""
